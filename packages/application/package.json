{
  "name": "@interviews-tool/application",
  "version": "1.0.0",
  "description": "Application layer — shared use cases for all server-side consumers (Elysia, TanStack serverFn, Astro, cron jobs)",
  "keywords": [
    "application",
    "server-only",
    "use-cases"
  ],
  "license": "ISC",
  "author": "",
  "files": [
    "dist",
    "src"
  ],
  "type": "module",
  "exports": {
    "./hiring": "./src/hiring/index.ts",
    "./interactions": "./src/interactions/index.ts",
    "./package.json": "./package.json"
  },
  "publishConfig": {
    "exports": {
      "./hiring": "./dist/hiring.mjs",
      "./interactions": "./dist/interactions.mjs",
      "./package.json": "./package.json"
    }
  },
  "scripts": {
    "build": "tsdown",
    "dev:watch": "tsdown --watch",
    "clean": "rm -rf dist",
    "check-types": "tsc --noEmit",
    "lint": "oxlint src",
    "lint:fix": "oxlint --fix src"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@interviews-tool/domain": "workspace:*",
    "@types/node": "^22.14.1",
    "tsdown": "^0.18.2",
    "typescript": "^5"
  },
  "packageManager": "bun@1.3.4"
}
